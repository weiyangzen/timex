# TimeX Stage 0 Todo Snapshot - 2026-05-01

Source of truth: [Docs/stage0_blueprint.md](stage0_blueprint.md)

This snapshot is generated for the local execution cron. Completion must be reconciled back to the blueprint and this todo snapshot only after runnable implementation and validation pass.

## Summary

- Total checklist items: 115
- Finished: 106
- Unfinished: 9
- Demo script steps: 41
- Validation posture: code + functional scripts + agentic lint

Progress note:

- `106/115 = 92.2%` is worker/main-repo verified progress, not final Stage 0 acceptance.
- Checked items are backed by isolated worker evidence and still require main-repo integration plus final validation.
- Worker 4 completed Discover and Calendar in isolation with lint/build/static checks.
- Worker 5 completed Trade/K-line in isolation with rule validation, lint/build, and HTTP smoke on `5012`.
- Worker 6 completed Conversation and browser-contained avatar fallback in isolation with lint/build/static layout checks.
- Worker 7 completed offline validation tooling in isolation; live keepalive remains pending until services run on `5012` and `5001`.
- Worker 8 completed agentic lint/reconciliation tooling in isolation and correctly refused checklist reconciliation without green merged evidence.
- Worker 1 completed backend/API in isolation with backend smoke, `go test`, `go vet`, and `go build`.
- Worker 2 completed frontend shell/shared primitives/contracts/routes in isolation with `npm ci`, lint/build, and HTTP route smoke on `5012`.
- Worker 3 completed auth UX and Personal Center in isolation with auth functional checks, lint/build, and HTTP smoke on `5013` because `5012` was occupied.
- Main repo cleanup removed the deprecated third-party agent connector/import prototype surface from frontend, README, and blueprint wording; product/docs search plus frontend lint/build passed.
- Main repo now integrates the frontend Stage 0 route shell for `/discover`, `/calendar`, `/calendar/[symbol]`, `/trade`, `/trade/[symbol]`, `/conversation`, `/conversation/[sessionId]`, and `/personal-center`; frontend lint/build passed on port-5012 Next config.
- Main repo verifies Calendar/Trade capacity/base-floor visibility and mobile Trade table collapse through integrated UI/CSS plus `npm run build`.
- Main repo now integrates the Go backend API on `5001`; `go test ./...`, `go vet ./...`, `go build ./cmd/api`, and `backend/scripts/smoke.sh` pass.
- Backend smoke verifies health/ready, anonymous reads, auth gate, local email/password register, issue/import, buy-seat, receipt, base floor, timing match, aggregation, demo history, session, and platform-managed mock runtime.
- Frontend dev server is live on `localhost:5012`; live route smoke passed for Discover, Calendar, Trade, Conversation, Personal Center, and dynamic detail routes.
- Main repo now has browser-contained avatar fallback auto blink, in-page gaze, and tap reaction; frontend lint/build pass.
- `shitagaki-lab/see-through` and Project AIRI are cloned under `.cron/research/`; see-through is a PSD/mask/depth decomposition pipeline, while AIRI's web Live2D stack expects already-prepared `model3/moc3` assets via Pixi/`pixi-live2d-display`.
- Chrome DevTools checks passed at 1440px desktop and 390px mobile emulation for Discover, Calendar, Trade, Conversation, and Personal Center with no horizontal overflow.
- `Docs/schema_stage0.sql` is now aligned with the backend Stage 0 object model; static table/field checks pass, but local psql execution is unavailable.
- Live2D see-through worker built an isolated `.cron/research/see-through/.venv` and got main/quantized/blockswap `--help` probes passing, but H08/H09 remain open: no real output was generated because HuggingFace model download failed with `No route to host` and the inference paths are CUDA-oriented on an MPS/no-CUDA host.
- Live2D AIRI worker integrated a model-package detection/fallback boundary in the frontend, but H10 remains open: `assets/live2d/generated` has no `model3.json`/`.moc3` package and no Pixi renderer has been render-verified.

## Worker Lanes

- Worker 1: backend foundation, health, auth, data model, demo store.
- Worker 2: frontend shell, shared UI primitives, API client, route scaffold.
- Worker 3: auth UX, Personal Center, seller setup, knowledge base import.
- Worker 4: Discover and Calendar capacity-window booking flow.
- Worker 5: Trade, receipt, market events, timing/aggregation/delay UX.
- Worker 6: Conversation, session access gate, topics, avatar/Live2D fallback.
- Worker 7: immersive demo data, validation scripts, service keepalive checks.
- Worker 8: agentic lint, blueprint/todo reconciliation, final integration checklist.

## Checklist Status

The implementation checklist under `Docs/stage0_blueprint.md#17-implementation-checklist` is partially worker-validated.

Completed in the current evidence tier:

- A01-A04
- B01-B10
- C01-C12
- D01-D03
- E01-E10
- F01-F17
- G01-G14
- H01, H02, H03, H04, H05, H06, H07, H11
- I01-I08
- J01, J02, J04, J05, J06, J09, J10, J12, J13, J14, J15, J16, J17, J19, J20, J22-J26

Still open:

- Schema alignment.
- Live2D see-through decomposition output for H08/H09.
- Real Live2D model package plus render verification for H10.
- Full integrated demo validation items that require merged frontend/backend running together.
- Full integrated demo validation on frontend `5012` and backend `5001`.

## Acceptance Gates

- Frontend runs on `localhost:5012` and stays alive during validation.
- Backend runs on `localhost:5001` and exposes green `/healthz` and `/readyz`.
- Functional validation script exercises anonymous browsing, protected action auth, capacity windows, base floor, ticket receipt, demo history, and conversation access gate.
- Frontend lint/build pass.
- Agentic lint verifies the Stage 0 product invariants in the blueprint.
